---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';

const getBlog = await fetch('http://localhost:4000');
const blogs = await getBlog.json();
console.log(blogs.getData);
---
<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<h1>🧑‍🚀 Hello, Astronaut! </h1>
			<p>
				Welcome to the official <a href="https://astro.build/">Astro</a> blog starter template. This
				template serves as a lightweight, minimally-styled starting point for anyone looking to build
				a personal website, blog, or portfolio with Astro.
			</p>
			<p><strong>Create postingan <code><a href="/post">click here</a></code> directory</strong></p>
			{
				blogs.getData.data.map((blog) => {
					return(<p>
						<h2>{blog.title}</h2>
						<p>{blog.content}</p>
					</p>)
				})
			}
		</main>
		<Footer />
	</body>
</html>
